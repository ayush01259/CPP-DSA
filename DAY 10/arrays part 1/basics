#include <iostream>
using namespace std;
int main() {
    int arr[15] = {7,5,2,8,1};
    int n = sizeof(arr) / sizeof(int);
    
    for (int i = 0; i <n; i++){
        cout<<arr[i] << " "  ; 
    }
    cout<<endl;
    return 0;
}